spring.application.name=Hotel Management

server.port=9000

#management.endpoints.web.base-path=/
#management.endpoints.web.exposure.include=info,health,metrics,mappings,beans,configprops,httptrace,threaddump,jolokia,prometheus,scheduledtasks,env
#management.server.port=9001

# CockroachDB.
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQL94Dialect
# Do not touch the Database.
spring.jpa.hibernate.ddl-auto=none
# Stupid thing that lets me name my own columns. TODO: Find better way...
spring.jpa.hibernate.naming_strategy=org.hibernate.cfg.EJB3NamingStrategy
